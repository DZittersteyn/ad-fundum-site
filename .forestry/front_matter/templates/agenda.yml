---
label: Agenda
hide_body: true
fields:
- type: field_group_list
  name: agenda
  label: agenda
  fields:
  - type: datetime
    name: date
    label: Date
    config:
      required: true
      date_format: DD-MM-YYYY
      export_format: YYYY-MM-DD
      time_format: " "
  - type: text
    name: title
    label: Title
    config:
      required: true
      max: 
  config:
    labelField: title
pages:
- _data/events.yml
